name: Hola mundo

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest

    steps:
      - name: hola mundo
        run: echo "Hola mundo desde la rama"

  other:
      needs: hola
      runs-on: ubuntu-latest

      steps:
        - name: Checkout
          uses: actions/checkout@v4.2.2
        - name: LS
          run: ls -al